{
  "mcpServers": {
    "claude-ops-investigator": {
      "command": "python",
      "args": [
        "-m",
        "claude_ops.mcp.server"
      ],
      "env": {
        "PYTHONPATH": "${PWD}/src"
      }
    }
  }
}
